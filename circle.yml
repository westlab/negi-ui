machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.2.0

test:
  pre:
    - bundle exec rubocop
    - ./node_modules/bower/bin/bower install
    - ./node_modules/coffeelint/bin/coffeelint app/assets/javascripts/
